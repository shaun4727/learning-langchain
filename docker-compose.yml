services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    networks:
      - agentic_network

  db:
    image: pgvector/pgvector:pg16
    container_name: agentic_postgres_db
    ports:
      - "5432:5432"
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - agentic_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: agentic_pgadmin_ui
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@mononsoft.com  # <--- CORRECT PLACE: Updated to use .com TLD
      PGADMIN_DEFAULT_PASSWORD: root
    depends_on:
      - db
    networks:
      - agentic_network

volumes:
  postgres_data:

networks:
  agentic_network:
    driver: bridge